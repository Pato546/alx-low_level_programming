#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{

	int first_num;

	for (first_num = '0'; first_num <= '9'; first_num++)
	{
		putchar(first_num);
	}

	putchar('\n');

	return (0);
}

